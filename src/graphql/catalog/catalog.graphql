query findFirstCatalog($where: CatalogWhereInput!) {
  findFirstCatalog(where: $where) {
    coverTitle
    coverImage
    footerTitle
    footerImage
    lists {
      id
      title
      coverImage
      icon
      sublists {
        id
        title
        coverImage
        icon
      }
    }
  }
}
